xs[::] # lower, upper, step all take the default values
xs[::2] # step is 2, lower and upper defaults to the beginning and end of the array
xs[:3] # first three elements
xs[3:] # elements from index 3
xs[-1] # the last element in the array
xs[-3:] # the last three elements
